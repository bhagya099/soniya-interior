const IMAGES = {
  logo: require("./logo.jpg"),
};

// Project/portfolio photos live under ./rooms/<room-folder>/ and are
// loaded automatically — see src/data/projects.js for how they're grouped.

export default IMAGES;
